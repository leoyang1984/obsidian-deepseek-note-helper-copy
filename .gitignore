# Dependencies
node_modules/

# Build outputs (Obsidian requires main.js and styles.css in the repo)
# main.js (Removed from ignore to allow plugin installation)
plugin-release（obsidian-deepseek-note-helper）/
plugin-release（obsidian-deepseek-note-helper）-V2.zip

# Obsidian local settings (where API Keys are stored)
test-vault/.obsidian/

# macOS system files
.DS_Store

# Potential secrets
.env
data.json
*.log
