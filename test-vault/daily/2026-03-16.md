1. 启动代理 (下载/更新前执行)

   输入：proxy_on

   效果：激活 10808 端口，解决 GitHub 连接超时或下载缓慢问题。

  

2. 执行安装 (常用命令)

   - 安装工具 (如 jq, git): brew install <名称>

   - 安装软件 (如 Chrome, Obsidian): brew install --cask <名称>

   - 更新所有软件: brew upgrade

   - 查找软件: brew search <名称>

  

3. 关闭代理 (完成后执行)

   输入：proxy_off

   效果：恢复终端默认网络环境，避免影响局域网或国内服务。












---

## 内容总结

这篇笔记记录了在 macOS 上使用 Homebrew 进行软件安装和更新时的代理配置流程：

### 主要步骤：
1. **启动代理**：使用 `proxy_on` 命令激活 10808 端口，解决 GitHub 连接超时或下载缓慢问题
2. **执行安装/更新操作**：
   - 安装命令行工具：`brew install <名称>`
   - 安装图形界面软件：`brew install --cask <名称>`
   - 更新所有软件：`brew upgrade`
   - 查找软件：`brew search <名称>`
3. **关闭代理**：使用 `proxy_off` 命令恢复终端默认网络环境，避免影响局域网或国内服务

### 核心要点：
- 这是一个完整的工作流程：启动代理 → 执行操作 → 关闭代理
- 专门针对需要访问 GitHub 等国外服务的场景
- 使用端口 10808 作为代理端口
- 强调操作完成后及时关闭代理，避免影响其他网络服务

这个流程对于在中国大陆使用 Homebrew 的用户特别有用，可以有效解决网络连接问题。

*总结生成时间：2026-03-16*
